# HUGGING FACE AI MODELS CONNECTION

## Deployment
For a better usage, it is a good option to use a virtual environment to install all dependencies.

python3 -m venv venv
source venv/bin/activate
pip install ... (or with requirements file: pip install -r requirements.txt)


## Executing the Streamlit server 
streamlit run UI.py
